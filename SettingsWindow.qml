import QtQuick
import QtQuick.Controls

Window {

    id: settings_window
    width: 600
    height: 700

    ScrollView {
        ListView {
            id: settings_points

            ListElement {
                name: appeareance

            }
        }
    }

    //Every point information
    Rectangle {

    }
}
